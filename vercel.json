{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/apps/trailcrafter/:path*",
      "destination": "/:path*"
    }
  ]
}
